# pare-static-mcp

Static-analysis MCP worker for PARE (Android APK). See design + plan in the PARE repo
under `docs/superpowers/`. v1: seven read-only tools (load_apk, find_symbol, grep_smali,
list_methods, extract_strings, decompile_method, read_manifest).
